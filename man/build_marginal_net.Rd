% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_marginal_net.R
\name{build_marginal_net}
\alias{build_marginal_net}
\title{Build a Signed Marginal Network from a Feature Matrix}
\usage{
build_marginal_net(
  x,
  corr_method = c("pearson", "spearman"),
  covariance_estimator = c("sample", "shrinkage", "auto"),
  covariance_check = c("auto", "always", "never"),
  shrinkage_args = list(),
  threshold_method = c("none", "absolute", "density", "pvalue"),
  threshold_value = NULL,
  adjust = c("none", "holm", "hochberg", "hommel", "bonferroni", "BH", "BY"),
  output = c("graph", "adjacency", "matrix", "both", "details")
)
}
\arguments{
\item{x}{Numeric matrix or data frame with samples in rows and features in columns.}

\item{corr_method}{Correlation flavor used to reconstruct the signed association matrix.}

\item{covariance_estimator}{Use \code{"sample"} for ordinary
sample correlation, \code{"shrinkage"} for shrinkage correlation, or \code{"auto"} to
start from the sample estimator and regularize automatically when the
covariance matrix is problematic.}

\item{covariance_check}{Control whether the covariance matrix is checked for
singularity or poor conditioning. Use \code{"always"} to always perform the check,
\code{"never"} to skip it, or \code{"auto"} to check only when needed for warnings or
automatic fallback selection.}

\item{shrinkage_args}{Optional named list or named vector of arguments forwarded to
\code{\link[corpcor:cov.shrink]{corpcor::cor.shrink()}}. Supported entries are \code{lambda}, \code{w}, and \code{verbose}.}

\item{threshold_method}{Thresholding rule applied after marginal reconstruction.
One of \code{"none"}, \code{"absolute"}, \code{"density"}, or \code{"pvalue"}.}

\item{threshold_value}{Either a single numeric threshold used with \code{threshold_method},
or a named list/vector with entries \code{abs}, \code{dens}, and/or \code{p}. When a named
value is supplied, the corresponding thresholded edge sets are intersected.}

\item{adjust}{Multiple-testing correction used when p-value thresholding is requested.}

\item{output}{One of \code{"graph"}, \code{"adjacency"}, \code{"matrix"}, \code{"both"}, or \code{"details"}.}
}
\value{
Depending on \code{output}, a signed graph, matrix, adjacency, or a detail list.
}
\description{
Reconstruct a signed marginal association matrix, optionally
apply thresholding, and return it as a matrix and/or graph. When
\code{covariance_estimator = "sample"} and covariance checking is enabled, a singular
or badly conditioned covariance matrix triggers a warning. When
\code{covariance_estimator = "auto"} and covariance checking is enabled, the function
warns and switches to shrinkage correlation instead.
}
\section{Shrinkage Covariance Estimation}{

When \code{covariance_estimator = "shrinkage"} or when \code{covariance_estimator = "auto"}
falls back to regularization, the function uses \code{\link[corpcor:cov.shrink]{corpcor::cor.shrink()}} to
stabilize the marginal association estimate. This can be useful when the sample
covariance is singular or badly conditioned, but it also introduces bias by
shrinking the association structure toward a more regular target.
}

\references{
Jin, Y., Notredame, C., & Erb, I. (2023). \emph{A unifying and general account
of symmetric power transformations for compositional data}. arXiv:2212.00496.
\url{https://arxiv.org/abs/2212.00496}
}
