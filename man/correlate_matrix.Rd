% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder_corr_netw.R
\name{correlate_matrix}
\alias{correlate_matrix}
\title{Calculate correlation matrix (and p-values on demand)}
\usage{
correlate_matrix(
  x,
  method = c("pearson", "spearman", "kendall"),
  what = c("cor", "cor_p")
)
}
\arguments{
\item{x}{Numeric matrix or data.frame (samples in rows, variables in columns).}

\item{method}{One of "pearson", "spearman", or "kendall".}

\item{what}{Either "cor" (default) or "cor_p" to compute both correlation and p-values.}
}
\value{
A list with:
\itemize{
\item \code{cor}: correlation matrix (always returned)
\item \code{p}: p-value matrix (only if \code{what = "cor_p"})
}
}
\description{
Compute correlation and optionally p-values from a numeric matrix.
If \code{what = "cor"}, uses base \code{cor()} for speed. If \code{what = "cor_p"}, uses \code{cor.test()}
for each pair. Zero-variance columns are excluded from computation and return NA.
}
