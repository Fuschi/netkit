% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_partial_net.R
\name{build_partial_net}
\alias{build_partial_net}
\title{Build a Signed Partial Network from a Feature Matrix}
\usage{
build_partial_net(
  x,
  transform = c("none", "rank"),
  covariance_estimator = c("sample", "shrinkage", "auto"),
  shrinkage_args = list(),
  lambda = NULL,
  nlambda = 20,
  lambda_min_ratio = 0.01,
  rep_num = 20,
  stars_thresh = 0.1,
  subsample_ratio = NULL,
  seed = NULL,
  glasso_args = list(),
  output = c("graph", "adjacency", "matrix", "both", "details"),
  verbose = FALSE
)
}
\arguments{
\item{x}{Numeric matrix or data frame with samples in rows and features in columns.}

\item{transform}{Optional preprocessing before covariance estimation. Use \code{"none"}
for raw values or \code{"rank"} to rank-transform each column first.}

\item{covariance_estimator}{Use \code{"sample"} for the sample covariance,
\code{"shrinkage"} for shrinkage covariance, or \code{"auto"} to start from the sample
covariance and regularize automatically when it is problematic.}

\item{shrinkage_args}{Optional named list or named vector of arguments forwarded to
\code{\link[corpcor:cor.shrink]{cov.shrink()}}. Supported entries are \code{lambda}, \code{lambda.var}, \code{w},
and \code{verbose}.}

\item{lambda}{Partial-network penalty. A single numeric value fits a single
graphical lasso model. \code{NULL} creates a default lambda path and selects a value
by StARS. A numeric vector is treated as an explicit lambda path for StARS.}

\item{nlambda}{Number of lambda values when \code{lambda = NULL}.}

\item{lambda_min_ratio}{Ratio between the smallest and largest lambda when
\code{lambda = NULL}.}

\item{rep_num}{Number of subsamples used by StARS when a lambda path is fitted.}

\item{stars_thresh}{StARS instability threshold.}

\item{subsample_ratio}{Optional subsampling ratio passed to \code{\link[pulsar:pulsar]{pulsar()}}.}

\item{seed}{Optional random seed used by StARS.}

\item{glasso_args}{Optional named list or named vector of arguments forwarded to
\code{\link[glasso:glasso]{glasso()}}. Use \code{lambda} rather than \code{glasso_args$rho}.}

\item{output}{One of \code{"graph"}, \code{"adjacency"}, \code{"matrix"}, \code{"both"}, or \code{"details"}.}

\item{verbose}{Logical; if \code{TRUE}, report the main estimation steps.}
}
\value{
Depending on \code{output}, a signed graph, matrix, adjacency, or a detail list.
}
\description{
Reconstruct a signed partial-correlation network from a feature matrix.
The function estimates a covariance matrix, fits a graphical lasso model, converts
the precision matrix to signed partial correlations, and returns the sparse partial
network directly from the glasso solution. When \code{lambda} is \code{NULL} or a numeric
vector, the function selects the penalty through StARS before fitting the final model.
}
\section{Shrinkage Covariance Estimation}{

When \code{covariance_estimator = "shrinkage"} or when \code{covariance_estimator = "auto"}
falls back to regularization, the function uses \code{\link[corpcor:cor.shrink]{cov.shrink()}} before
graphical lasso estimation. This stabilizes the covariance matrix in singular or
badly conditioned settings but can also bias the resulting partial network.
}

\references{
Jin, Y., Notredame, C., \& Erb, I. (2023). \emph{A unifying and general account
of symmetric power transformations for compositional data}. arXiv:2212.00496.
\url{https://arxiv.org/abs/2212.00496}
}
