% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder_corr_netw.R
\name{build_corr_net_abs_p}
\alias{build_corr_net_abs_p}
\title{Build correlation network with |cor| and p-value thresholds}
\usage{
build_corr_net_abs_p(
  x,
  cor_method = c("pearson", "spearman", "kendall"),
  min_cor,
  alpha = 0.01,
  adjust = c("none", "holm", "hochberg", "hommel", "bonferroni", "BH", "BY", "fdr"),
  output = c("graph", "adjacency", "both")
)
}
\arguments{
\item{x}{Numeric matrix or data.frame (samples in rows, variables in columns).}

\item{cor_method}{Correlation method: "pearson", "spearman", or "kendall".}

\item{min_cor}{Minimum absolute correlation to retain an edge (in [0, 1]).}

\item{alpha}{Significance level for p-values (in (0, 1]).}

\item{adjust}{P-value adjustment method (see \code{threshold_abs_pvalue}).}

\item{output}{"graph", "adjacency", or "both".}
}
\value{
Depending on \code{output}:
\itemize{
\item "graph": an igraph object;
\item "adjacency": numeric adjacency matrix;
\item "both": list with elements \code{adjacency} and \code{graph}.
}
}
\description{
High-level wrapper that:
\enumerate{
\item computes pairwise correlations and p-values;
\item keeps edges with |cor| >= min_cor AND adjusted p-value <= alpha;
\item converts the resulting adjacency matrix into an igraph object.
}
}
