# netkit

`netkit` is an R package for signed network reconstruction from already
processed feature matrices.

Current scope:

- signed marginal reconstruction with sample or shrinkage estimators
- sign-preserving thresholding
- signed clustering and signed-aware layouts
